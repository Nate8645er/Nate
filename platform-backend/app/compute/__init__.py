"""compute/ — Hardware-Abstraktion (HAL).

Neue Hardware = neue ComputeBackend-Implementierung + Eintrag in der Erkennung.
Sonst nichts. Nichts im System darf voraussetzen, dass eine GPU existiert; der
CPU-Pfad ist vollwertig (Auftrag §5).
"""
