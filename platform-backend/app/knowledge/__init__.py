"""knowledge/ — RAG, Memory, Wissensgraph (Phase 3).

Geplante Interfaces: VectorStore (Qdrant-Adapter), Embedder (batched),
Retriever (mit Reranking), MemoryStore (Postgres/gedaechtnis + optional pgvector).
Noch nicht implementiert — bewusst leer, um kein Schein-Feature vorzutäuschen.
"""
