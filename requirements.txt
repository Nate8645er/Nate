# Jarvis - KI-Assistent für Windows 11
# Schritt 1: Nur die Basis-Abhängigkeit für die Ollama-Verbindung.
# Weitere Pakete (Sprache, Vision, etc.) kommen in späteren Schritten dazu.

requests>=2.32.0
PyYAML>=6.0

# Optionales Cloud-Gehirn (Claude API von Anthropic)
anthropic>=0.116.0

# Sprachmodul (Sprache zu Text, Text zu Sprache)
pyttsx3>=2.90
SpeechRecognition>=3.10
sounddevice>=0.4.6

# Nur für die Entwicklung: Tests ausführen mit `python -m pytest`
pytest>=8.0
