<section class="page-404">
  {% render 'wasserblasen' %}
  <div>
    <div class="lost-sub">{% render 'katze-flasche' %}</div>
    <h1>Hier ist der Napf leer.</h1>
    <p>Die Seite, die du suchst, gibt es nicht oder nicht mehr.</p>
    <a href="{{ routes.root_url }}" class="btn btn-coral magnetic">Zurück zum Start</a>
  </div>
</section>
