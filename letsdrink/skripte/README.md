# Skripte

Python 3 mit Pillow und numpy:

    pip install pillow numpy

Oben in jeder Datei BASE und OUT auf deine Pfade anpassen.
Die Fonts liegen im Ordner fonts, BASE muss dorthin zeigen.

Kernbaustein: recolor() in build_scene.py erzeugt aus der grossen
Produktaufnahme alle sechs Farben in voller Aufloesung.
