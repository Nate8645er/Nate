# Packaging-Hinweis

Dieses Paket wurde unveraendert uebernommen. Ergaenzt wurde ausschliesslich:

- `.claude-plugin/plugin.json` — das Manifest, das der Plugin-Upload verlangt.

Kein SKILL.md, kein README und kein Code wurde veraendert.
Der Wrapper-Ordner `threejs-skills-main/` wurde beim Packen entfernt,
damit `.claude-plugin/` auf oberster ZIP-Ebene liegt.
