{%- comment -%}
  sections/a-sammlung.liquid — LET'SDRINK AMBER, Kategorie und Kategorieliste
  9.8.2026. Bauleitung.

  Dieser Shop hat ein einziges Produkt. Kategorieseiten sind deshalb
  Randseiten - sie muessen funktionieren und dazugehoeren, mehr nicht.
  Dieselbe Datei bedient beide Faelle: liegt eine Kategorie vor, zeigt
  sie deren Produkte, sonst die Liste aller Kategorien.
{%- endcomment -%}

<div class="a-seite">
  <div class="a-huelle">

    {%- if collection -%}

      <div class="a-kopfzeile">
        <p class="a-auge">Let'sDrink</p>
        <h1 class="a-titel">{{ collection.title }}</h1>
        {%- if collection.description != blank -%}
          <div class="a-vorspann">{{ collection.description }}</div>
        {%- endif -%}
      </div>

      {%- paginate collection.products by 24 -%}
        {%- if collection.products.size == 0 -%}
          <div class="a-leer">
            <p style="margin-bottom:1.8rem">In dieser Kategorie liegt zurzeit nichts.</p>
            <a class="a-knopf a-knopf--schmal" href="{{ routes.root_url }}">Zur Flasche</a>
          </div>
        {%- else -%}
          <ul class="a-gitter">
            {%- for p in collection.products -%}
              {%- liquid
                assign eigen = false
                if p.handle == 'trinkflasche-napf-hund-katze'
                  assign eigen = true
                endif
              -%}
              <li>
                <a class="a-karte" href="{{ p.url }}">
                  <div class="a-karte__bild">
                    {%- if eigen -%}
                      <img src="{{ 'a-flasche-tuerkis.webp' | asset_url }}" alt=""
                           width="650" height="2212" loading="lazy" decoding="async">
                    {%- elsif p.featured_image -%}
                      <img src="{{ p.featured_image | image_url: width: 520 }}" alt=""
                           width="520" height="520" loading="lazy" decoding="async">
                    {%- endif -%}
                  </div>
                  <h2>{{ p.title }}</h2>
                  {%- comment -%} Katalogpreis CHF 39.90 stand hier, gezahlt wird
                     aber der reduzierte Betrag. Gleiche Rechnung
                     wie ueberall sonst im Theme. {%- endcomment -%}
                  {%- assign p_ab = p.price | times: 95 | plus: 99 | divided_by: 100 -%}
                  <p>{{ p_ab | money }}</p>
                </a>
              </li>
            {%- endfor -%}
          </ul>

          {%- if paginate.pages > 1 -%}
            <ul class="a-blaettern">
              {%- for teil in paginate.parts -%}
                <li>
                  {%- if teil.is_link -%}
                    <a href="{{ teil.url }}">{{ teil.title }}</a>
                  {%- else -%}
                    <span {% if teil.title == paginate.current_page %}aria-current="page"{% endif %}>{{ teil.title }}</span>
                  {%- endif -%}
                </li>
              {%- endfor -%}
            </ul>
          {%- endif -%}
        {%- endif -%}
      {%- endpaginate -%}

    {%- else -%}

      <div class="a-kopfzeile">
        <p class="a-auge">Let'sDrink</p>
        <h1 class="a-titel">Kategorien</h1>
      </div>

      <ul class="a-gitter">
        {%- for k in collections -%}
          <li>
            <a class="a-karte" href="{{ k.url }}">
              <div class="a-karte__bild">
                {%- if k.image -%}
                  <img src="{{ k.image | image_url: width: 520 }}" alt="" width="520" height="520"
                       loading="lazy" decoding="async">
                {%- elsif k.products.first.featured_image -%}
                  <img src="{{ k.products.first.featured_image | image_url: width: 520 }}" alt=""
                       width="520" height="520" loading="lazy" decoding="async">
                {%- endif -%}
              </div>
              <h2>{{ k.title }}</h2>
              <p>{{ k.all_products_count }} Artikel</p>
            </a>
          </li>
        {%- endfor -%}
      </ul>

    {%- endif -%}

    <div class="a-weiter">
      <a href="{{ routes.root_url }}">Zur Flasche</a>
      <a href="/pages/haeufige-fragen">Häufige Fragen</a>
      <a href="/pages/contact">Kontakt</a>
    </div>

  </div>
</div>

{% schema %}
{
  "name": "Amber Kategorie",
  "settings": [],
  "presets": [{ "name": "Amber Kategorie" }]
}
{% endschema %}
